// Import Bootstrap JavaScript (includes Popper.js for tooltips, dropdowns, etc.)
import 'bootstrap';

// Import custom SCSS file that also includes Bootstrap CSS
import './custom.scss';

// Import custom JavaScript (optional)
import './custom.js';